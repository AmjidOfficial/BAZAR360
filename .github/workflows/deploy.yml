# ... keep everything above the same ...

      - name: Install dependencies
        run: npm ci
        
      - name: Build Website
        env:
          NODE_ENV: production  # <-- Add this line for Tailwind v4 compatibility
        run: npm run build
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist'

      # ... keep everything below the same ...
